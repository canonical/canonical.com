import { DotLottie } from '@lottiefiles/dotlottie-web';

const homepageSuru = new DotLottie({
    autoplay: true,
    loop: true,
    canvas: document.querySelector('#hero-section-suru'),
    src: "https://assets.ubuntu.com/v1/8a75d42d-suru_light.json", // or .json file
});